import Loader from "./common/Loader"
import LoaderAnim from "./common/LoaderAnim"

const Ticket = (props) => {
    return (
        <div>
            <div>
                <Loader />
            </div>
        </div>
    )
}

export default Ticket